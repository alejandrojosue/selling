---
import Layout from "../layouts/Layout.astro";
const lang = Astro.cookies.get("lang")?.value || "es";
---

<Layout>
  <section class="py-20 px-6 text-center max-w-3xl mx-auto">
    <h1 class="text-4xl font-bold text-gray-800 mb-4">
      {lang === "es"
        ? "Sitios web listos para ti, sin complicaciones"
        : "Ready-to-use websites, no hassle"}
    </h1>

    <p class="text-lg text-gray-600 mb-8">
      {lang === "es"
        ? "Elige una plantilla, envíanos tus datos y nosotros nos encargamos del resto. Diseño profesional, rápido y asequible."
        : "Choose a template, send us your content, and we’ll handle the rest. Professional, fast, and affordable websites."}
    </p>

    <a
      href="/gallery"
      class="inline-block bg-indigo-500 hover:bg-indigo-600 text-white font-semibold px-6 py-3 rounded-lg transition"
    >
      {lang === "es" ? "Ver Plantillas Disponibles" : "Browse Available Templates"}
    </a>
  </section>

  <section class="bg-gray-50 py-16 px-4">
    <div class="max-w-4xl mx-auto grid md:grid-cols-3 gap-8 text-center">
      <div>
        <h3 class="text-xl font-semibold text-gray-700 mb-2">
          {lang === "es" ? "1. Elige una plantilla" : "1. Pick a template"}
        </h3>
        <p class="text-gray-600">
          {lang === "es"
            ? "Revisa nuestra galería y selecciona el diseño que más te guste."
            : "Browse our gallery and choose a design you like."}
        </p>
      </div>
      <div>
        <h3 class="text-xl font-semibold text-gray-700 mb-2">
          {lang === "es" ? "2. Envíanos tus datos" : "2. Send your info"}
        </h3>
        <p class="text-gray-600">
          {lang === "es"
            ? "Imágenes, textos y colores preferidos. Nosotros lo adaptamos todo."
            : "Images, text, and your preferred colors. We’ll do the rest."}
        </p>
      </div>
      <div>
        <h3 class="text-xl font-semibold text-gray-700 mb-2">
          {lang === "es" ? "3. Publicamos tu sitio" : "3. We publish it"}
        </h3>
        <p class="text-gray-600">
          {lang === "es"
            ? "Recibe tu sitio listo, con hosting, correo y dominio gratis por un año."
            : "Your site goes live with hosting, email, and free domain for one year."}
        </p>
      </div>
    </div>
  </section>
</Layout>
