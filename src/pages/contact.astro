---
import Layout from "../layouts/Layout.astro";

const lang = Astro.cookies.get("lang")?.value || "es";
---

<Layout>
  <div class="max-w-4xl mx-auto px-4 py-12 text-gray-800">
    <h1 class="text-3xl md:text-4xl font-bold text-center mb-6">
      {lang === "es" ? "Contáctanos" : "Contact Us"}
    </h1>

    <p class="text-center text-lg mb-10">
      {
        lang === "es"
          ? "¿Tienes dudas, sugerencias o deseas empezar tu sitio personalizado? Envíanos un mensaje y te responderemos pronto."
          : "Have questions, suggestions, or want to start your custom website? Send us a message and we’ll get back to you soon."
      }
    </p>

    <div class="flex flex-col md:flex-row gap-8">
      <!-- Formulario -->
      <form
        method="POST"
        action="#"
        onsubmit="event.preventDefault(); alert('Formulario de ejemplo. No se envía aún.')"
        class="w-full md:w-2/3 bg-white p-6 rounded-2xl shadow border border-gray-200 space-y-6"
      >
        <div>
          <label for="name" class="block font-medium mb-1">
            {lang === "es" ? "Nombre" : "Name"}
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-400 transition"
          />
        </div>

        <div>
          <label for="email" class="block font-medium mb-1">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-400 transition"
          />
        </div>

        <div>
          <label for="message" class="block font-medium mb-1">
            {lang === "es" ? "Mensaje" : "Message"}
          </label>
          <textarea
            id="message"
            name="message"
            rows="5"
            required
            class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-400 transition resize-none"
          ></textarea>
        </div>

        <button
          type="submit"
          class="bg-indigo-500 hover:bg-indigo-600 text-white font-medium px-6 py-2 rounded-lg transition"
        >
          {lang === "es" ? "Enviar mensaje" : "Send Message"}
        </button>
      </form>

      <!-- Información de contacto -->
      <div
        class="w-full md:w-1/3 bg-white p-6 rounded-2xl shadow border border-gray-200 space-y-6"
      >
        <div class="flex items-start gap-4">
          <span class="text-indigo-500 text-xl">📧</span>
          <div>
            <p class="font-semibold">
              {lang === "es" ? "Correo electrónico" : "Email"}
            </p>
            <a
              href="mailto:josued.ad13@gmail.com"
              class="text-indigo-600 hover:underline break-all"
            >
              josued.ad13@gmail.com
            </a>
          </div>
        </div>

        <div class="flex items-start gap-4">
          <span class="text-indigo-500 text-xl">📱</span>
          <div>
            <p class="font-semibold">
              {lang === "es" ? "WhatsApp" : "WhatsApp"}
            </p>
            <a
              href="https://wa.me/50433971213"
              target="_blank"
              class="text-indigo-600 hover:underline"
            >
              (+504) 3397-1213
            </a>
          </div>
        </div>

        <div class="flex items-start gap-4">
          <span class="text-indigo-500 text-xl">⏰</span>
          <div>
            <p class="font-semibold">
              {lang === "es" ? "Horario de atención" : "Support hours"}
            </p>
            {
              lang === "es" ? (
                <>
                  <p class="font-semibold text-gray-500">Lunes a viernes</p>
                  <p class="text-sm">09:00 AM a 07:00 PM</p>
                </>
              ) : (
                <>
                  <p class="font-semibold text-gray-500">Monday to Friday</p>
                  <p class="text-sm">09:00 AM to 07:00 PM</p>
                </>
              )
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>
